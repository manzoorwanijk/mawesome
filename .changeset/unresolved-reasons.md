---
'@mawesome/dependency-audit': minor
---

Runtime `unresolved` findings now carry a `reason` (`subpath-not-exported`, `file-missing`, or `condition-mismatch`) pinpointing the cause, including ESM/CJS export-condition mismatches.
