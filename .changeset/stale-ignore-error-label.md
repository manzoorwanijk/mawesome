---
'@mawesome/dependency-audit': patch
---

Report a stale ignore rule as a red `error:` under `--fail-unused-ignores` (where it fails the run) instead of a plain `warning:`, and color stderr diagnostics to match the rest of the output.
