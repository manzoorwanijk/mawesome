---
title: mawesome
description: Docs and interactive playgrounds for the @mawesome/* tools.
template: splash
hero:
  tagline: Sharp, single-purpose tools for npm package authors — with live, in-browser playgrounds.
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import { tools } from '../../tools.ts';

<CardGrid>
	{tools.map((tool) => (
		<Card title={tool.name} icon={tool.icon}>
			<p>{tool.tagline}</p>
			<p>
				<a href={`/${tool.slug}/`}>Documentation →</a>
				{tool.playground && <Fragment> · <a href={`/${tool.slug}/playground/`}>⚡ Playground →</a></Fragment>}
			</p>
		</Card>
	))}
</CardGrid>
